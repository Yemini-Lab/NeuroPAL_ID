function handle = app()
    % Moved to Program.ProgramInfo.app. Delete this file once you have
    % time to confirm that this function has no leftover references
    % anywhere.
    handle = Program.ProgramInfo.app;
end
