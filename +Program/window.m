function handle = window()
    % Moved to Program.ProgramInfo.window. Delete this file once you have
    % time to confirm that this function has no leftover references
    % anywhere.
    handle = Program.ProgramInfo.window;
end
